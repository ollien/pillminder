import PillminderForm from "pillminder-webclient/src/pages/login/PillminderForm";
import React from "react";

const LoginForm = () => {
	return (
		<PillminderForm
			onAccessCode={() => {
				// TODO: stub, should transition to the next form
			}}
		/>
	);
};

export default LoginForm;
